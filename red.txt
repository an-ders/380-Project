HSV Ranges for RED at crosshair:

For use in code (RED):
'lower': np.array([60, 136, 167]),
'upper': np.array([173, 255, 255])

HSV Ranges for RED at crosshair:

For use in code (RED):
'lower': np.array([60, 129, 172]),
'upper': np.array([176, 255, 255])

HSV Ranges for RED at crosshair:

For use in code (RED):
'lower': np.array([60, 129, 174]),
'upper': np.array([175, 255, 255])

HSV Ranges for RED at crosshair:

For use in code (RED):
'lower': np.array([60, 132, 178]),
'upper': np.array([173, 255, 255])

HSV Ranges for RED at crosshair:

For use in code (RED):
'lower': np.array([60, 130, 178]),
'upper': np.array([175, 255, 255])

HSV Ranges for RED at crosshair:

For use in code (RED):
'lower': np.array([60, 134, 164]),
'upper': np.array([179, 255, 255])

HSV Ranges for RED at crosshair:

For use in code (RED):
'lower': np.array([60, 136, 165]),
'upper': np.array([175, 255, 255])

HSV Ranges for RED at crosshair:

For use in code (RED):
'lower': np.array([60, 143, 182]),
'upper': np.array([175, 255, 255])

HSV Ranges for RED at crosshair:

For use in code (RED):
'lower': np.array([60, 109, 202]),
'upper': np.array([163, 255, 255])

HSV Ranges for RED at crosshair:

For use in code (RED):
'lower': np.array([60, 99, 209]),
'upper': np.array([162, 255, 255])

HSV Ranges for RED at crosshair:

For use in code (RED):
'lower': np.array([60, 100, 202]),
'upper': np.array([164, 255, 255])